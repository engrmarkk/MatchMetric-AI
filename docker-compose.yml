services:
  api:
    build: .
    ports:
      - "8001:8001"
    volumes:
      - .:/app
    env_file:
      - .env
    command: gunicorn resumeai_proj.asgi:application --bind 0.0.0.0:8001 --worker-class uvicorn.workers.UvicornWorker --reload